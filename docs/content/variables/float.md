# Float

Integer is a data type in El that holds a decimal value.

If in your school (math lesson), decimal using coma `,`. In El, decimal using dot `.`

Example:

```el
10.29827
```

## Assignment

Using equals `=`:

```el
float_var = 123.456;
```