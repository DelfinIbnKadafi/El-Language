# El Language

El Language is a high-level programming language designed with a custom Virtual Machine (VM) architecture.

The language focuses on simplicity, productivity, and portability while providing an independent execution environment through its Virtual Machine.

## Architecture

El Language programs are compiled and executed through the El Virtual Machine.

The Virtual Machine handles program execution, memory management, and runtime operations, allowing El Language applications to run consistently across different systems.

## Features

- High-level programming language
- Custom Virtual Machine (VM)
- Direct VM execution
- Cross-platform runtime
- Simple and readable syntax
- Automatic memory management
- Extensible architecture
- Open source

## Code Example

```el
print "Hello, World!";
print "This is El Virtual Machine";

var int integer = 10;
print integer;
print integer + 10;
integer = 10;
print integer;

var str string = "Very Impresive Language!";
var bool boolean = true;
var float decimal;

print string;
print boolean;

decimal = 1.0;

var float el;
el = integer / (decimal * 10);
print el;

if(integer == 10 and el >= 1) =
  print "Integer == 10";
  if(integer > 0 or integer < 11) =
    print "Integer between 0 and 11";
else if(integer < 10) =
  print "Integer < 10";
else =
  print "Integer isn't 10";

exit;
```

## License

This project is open source and available for developers who want to explore, contribute, and improve the language.
